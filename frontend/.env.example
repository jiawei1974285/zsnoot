# 知枢前端环境变量示例
#
# 使用方式：复制为 .env 或 .env.local（Vite 会自动读取，.env.local 不入 git）
#
# ─── 模式 1：单体（默认） ───
# 不设置任何 VITE_* 变量。前端走 same-origin，由 vite dev proxy 或同源后端转发。
# 与改造前行为完全一致，无需任何配置。
#
# ─── 模式 2：云本机分离（P1 起支持） ───
# 设置以下两项（或其一）：
#
# VITE_CLOUD_API   = 云端控制面 baseURL（必填，启用分离模式的开关）
# VITE_LOCAL_API   = 本机 agent baseURL（不填默认 same-origin，dev 配合 vite proxy）
#
# 示例（开发环境，云端在本机另起 5005，本机 agent 还在 5004，前端 5174）：
#
#   VITE_CLOUD_API=http://127.0.0.1:5005
#   VITE_LOCAL_API=http://127.0.0.1:5004
#
# 示例（生产，云端发到 cloud.example.com，本机 agent 仍是 127.0.0.1:5004）：
#
#   VITE_CLOUD_API=https://cloud.example.com
#   VITE_LOCAL_API=http://127.0.0.1:5004
#
# 注意：分离模式下浏览器会跨域调用本机 agent，需要后端 app.py 配置
#       MJQ_LOCAL_CORS_ORIGINS 把前端域名加入白名单。
